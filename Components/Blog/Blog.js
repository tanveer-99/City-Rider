import React from 'react';
import Navbar from '../Navbar/Navbar';

const Blog = () => {
    return (
        <div className="container">
            <Navbar></Navbar>
        </div>
    );
};

export default Blog;